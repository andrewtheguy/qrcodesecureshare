[package]
name = "fast-qr-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
fast_qr = { version = "0.13.1", features = ["image"] }

[profile.release]
opt-level = "z"
lto = true
