# UI components
use https://ui.shadcn.com/ to generate components
for example, to add accordion, run npx shadcn@latest add accordion
no backward compatibility is needed for any changes
code freeze src/components/FountainQRReceiverLegacy.tsx, src/components/FountainQRSenderLegacy.tsx, and src/components/SequentialQR*.tsx, don't make changes to these files unless it is needed for the rest of the system to work